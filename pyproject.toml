[project]
name = "fitops-engine"
version = "0.1.0"
description = "Local-first workout MLOps platform"
requires-python = ">=3.11"
dependencies = [
  "openpyxl==3.1.5",
]

[project.optional-dependencies]
dev = [
  "pytest==8.3.3",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
